<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="16dp"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".Control"
    android:clickable="false">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Connect"
        android:drawableLeft="@drawable/link"
        android:id="@+id/button_connect"
        android:layout_gravity="center"
        android:background="@drawable/button_select"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/button_disconnect"
        android:drawableLeft="@drawable/download"
        android:text="Read Program List"
        android:id="@+id/button_proglist"
        android:layout_gravity="center_horizontal|top"
        android:background="@drawable/button_select"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/button_connect"
        android:drawableLeft="@drawable/link_broken"
        android:text="Disconnect"
        android:id="@+id/button_disconnect"
        android:layout_gravity="center_horizontal|bottom"
        android:background="@drawable/button_select"/>
</RelativeLayout>
